# probably don't need the create-master-key command anymore if we're 
# going to use just the "master password" to decrypt the configuration
# which will also have the keystore passwords...
# com.intel.kms.console.cmd.CreateMasterKey
com.intel.kms.console.cmd.GeneratePassword
